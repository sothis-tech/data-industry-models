# Overrides para entorno de producción
# Se carga encima de config.env cuando BACKEND_ENV_FILE=config.prod.env.

# Timeouts más conservadores para producción.
AGENT_TIMEOUT_TOTAL_SEC=30
AGENT_TIMEOUT_CONNECT_SEC=10
